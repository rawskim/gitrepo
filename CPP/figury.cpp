/*
 * figury.cpp
 */

#include <iostream>

using namespace std;

void prostokat2(int a, int b, znak) {
    for (int i = 1; i <= a; i++;) {
        for (int j = 1; j <= b; j++) {
            if ()
        }
    }
}

int main(int argc, char **argv)
{
    int a, b; // deklareacja
    a = b = 0; // inicjacja
    cout << "Podaj bok 1: ";
    cin >> a;
    cout << "Podaj bok 2: ";
    cin >> b;
    
    char znak;
    cout << "Podaj znak wypełnienia: ";
    cin >> znak;
    
    prostokat2(a, b, znak);
    return 0;
}

